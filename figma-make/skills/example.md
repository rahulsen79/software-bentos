---
name: example-skill
description: Use when <the trigger>. Produces <the output>. Example: "Use when turning a Figma frame into a component documentation draft."
---

# Example Skill

## Inputs
- What this needs to run (a frame image, a token file, a component name).

## Steps
1. State the task plainly.
2. State observed facts plainly; prefix proposals with "Recommended (confirm): ".
3. Use only names provided. Never invent component or token names.

## Output
- The exact shape of the result (sections, format), so it is consistent every run.
